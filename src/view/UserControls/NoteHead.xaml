<UserControl x:Class="tunelint.view.UserControls.NoteHead"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tunelint.view.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="10" d:DesignWidth="10">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <local:BlackNoteHead x:Name="BlackHead" Fill="Black"/>
    <local:WhiteNoteHead x:Name="WhiteHead" Fill="Black"/>
    <local:WholeNote x:Name="WholeHead" Fill="Black"/>
  </Grid>
</UserControl>
